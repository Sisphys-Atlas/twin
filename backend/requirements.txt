# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.13.2

# Config & utilities
pydantic-settings==2.4.0
python-dotenv==1.0.1
python-multipart==0.0.9
aiofiles==23.2.1

# AI — Gemini (summaries + chat agent, M3/M4)
google-generativeai==0.8.3

# AI — OpenAI (reserved for Whisper if added later)
openai==1.47.0

# NLP / text processing
langdetect==1.0.9
pypdf==4.3.1

# Numeric (cosine similarity for embeddings)
numpy==1.26.4

# Async HTTP — bridge communication
httpx==0.27.0

# Auth — JWT + password hashing
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
